121212313
스테이징 영역과 워킹 디렉터리의 차이는 무엇인가요? 두 영역을 분리했을 때 어떤 이점이 있나요?

워킹디렉터리엔 내가 현재 수정한 파일들이 뜹니다. 스테이징 영역엔 내가 커밋에 포함시킬 파일이 있습니다. / 두 영역을 분리했을때 장점은 원하는 파일만 커밋에 포함시킬 수 있습니다.

In working directory, shows only being chaged files. In staging area, which has committed files./ When separate two areas, the advantage is can include files which you want

branch problem.

git 브랜치 자동 머지의 장단점은?
장점: 빠르게 머지를 진행할 수 있다.
단점: 원치않는 코드가 겹칠 수 있다.
